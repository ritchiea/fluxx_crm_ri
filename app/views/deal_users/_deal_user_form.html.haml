= form.input :deal_id, :as => :hidden
= form.input :user, :label => "User", :as => :autocomplete, :autocomplete_url => users_path(:format => :json), :related_attribute_name => :full_name

= form.commit_button
