Fluxx CRM RI uses the fluxx_engine and fluxx_crm libraries.

It adds in organizations, users, org/users links, groups, favorites on top of the fluxx engine layer.

SETUP:
rake generate fluxx_engine_public